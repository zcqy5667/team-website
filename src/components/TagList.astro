---
// 标签和简短能力说明共用的展示列表。
// 传入空数组时不渲染内容，调用方不需要额外写条件判断。
type Props = {
  items: string[];
  className: string;
};

const { items, className } = Astro.props;
---

{items.length > 0 && <ul class={className}>{items.map((item) => <li>{item}</li>)}</ul>}
